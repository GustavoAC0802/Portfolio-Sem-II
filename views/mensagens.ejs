<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Mensagens Recebidas</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-white min-h-screen flex flex-col items-center justify-start p-6">
  
  <!-- Botão de voltar -->
  <div class="w-full flex justify-start mb-6">
    <a href="/" class="text-sm bg-purple-600 hover:bg-purple-700 px-4 py-2 rounded">
      Voltar
    </a>
  </div>

  <h1 class="text-3xl font-bold mb-8 text-center">Mensagens Recebidas</h1>

  <% if (mensagens.length === 0) { %>
    <p class="text-gray-400">Nenhuma mensagem encontrada.</p>
  <% } else { %>
    <div class="w-full max-w-4xl space-y-6">
      <% mensagens.forEach(mensagem => { %>
        <div class="bg-gray-800 rounded-lg p-6 shadow-md">
          <h2 class="text-xl font-semibold mb-2"><%= mensagem.nome %></h2>
          <p class="text-sm text-gray-400 mb-1"><strong>Email:</strong> <%= mensagem.email %></p>
          <p class="mb-4"><strong>Assunto:</strong> <%= mensagem.assunto %></p>

          <div class="flex space-x-4">
            <a href="/editar/<%= mensagem.id %>" class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded">
              Editar
            </a>
            <a href="/deletar/<%= mensagem.id %>" class="bg-red-600 hover:bg-red-700 text-white px-4 py-2 rounded"
               onclick="return confirm('Tem certeza que deseja deletar esta mensagem?')">
              Deletar
            </a>
          </div>
        </div>
      <% }) %>
    </div>
  <% } %>

</body>
</html>

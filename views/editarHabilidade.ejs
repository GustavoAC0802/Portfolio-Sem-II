<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>Editar Habilidade</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-white flex items-center justify-center min-h-screen relative">

  <!-- Botão de Voltar -->
  <a href="/habilidades"
    class="absolute top-4 left-4 text-white bg-purple-600 hover:bg-purple-700 px-4 py-2 rounded shadow">
    Voltar
  </a>

  <!-- Formulário de Edição -->
  <form action="/habilidades/editar/<%= habilidade.id %>" method="POST"
    class="bg-gray-800 p-8 rounded shadow-md w-full max-w-md space-y-4">
    <h1 class="text-2xl font-bold text-center mb-4">Editar Habilidade</h1>

    <div>
      <label for="nome" class="block mb-1">Nome da Habilidade</label>
      <input type="text" id="nome" name="nome" value="<%= habilidade.nome %>" class="w-full p-2 rounded bg-gray-700 border border-gray-600" required>
    </div>

    <div>
      <label for="categoria" class="block mb-1">Categoria</label>
      <select id="categoria" name="categoria" class="w-full p-2 rounded bg-gray-700 border border-gray-600" required>
        <option value="Programação" <%= habilidade.categoria === 'Programação' ? 'selected' : '' %>>Programação</option>
        <option value="Ferramentas" <%= habilidade.categoria === 'Ferramentas' ? 'selected' : '' %>>Ferramentas</option>
      </select>
    </div>

    <div>
      <label for="icone" class="block mb-1">Caminho ou URL do Ícone</label>
      <input type="text" id="icone" name="icone" value="<%= habilidade.icone %>" class="w-full p-2 rounded bg-gray-700 border border-gray-600" required>
    </div>

    <div class="text-center">
      <button type="submit" class="bg-blue-600 hover:bg-blue-700 px-4 py-2 rounded font-semibold">
        Atualizar Habilidade
      </button>
    </div>
  </form>

</body>
</html>
